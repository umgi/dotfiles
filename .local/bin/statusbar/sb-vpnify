#!/bin/sh

[ -n "$(pidof openvpn)" ] && status="🔒" || exit 0
response=$(vpnify curl -s ipinfo.io --silent --connect-timeout 3 --max-time 3)
[ $? != 0 ] && down="🔴"
"$(echo "$response" | grep -q "RU")" && down="🔴"

printf "%s%s" $status $down
